{
  "name": "spfx-data-kit",
  "version": "0.2.0",
  "description": "Dependency-free TypeScript data transport and SharePoint/Graph adapters.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
    "./capabilities": { "types": "./dist/manifest.d.ts", "import": "./dist/manifest.js" }
  },
  "files": ["dist", "README.md", "LICENSE", "SPEC.md"],
  "scripts": { "build": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true });\" && tsc -p tsconfig.json", "test": "tsx --test test/*.test.ts", "check": "tsc -p tsconfig.json --noEmit", "pack": "npm pack --dry-run" },
  "devDependencies": { "@types/node": "^22.0.0", "tsx": "^4.19.0", "typescript": "^5.6.0" }
}
